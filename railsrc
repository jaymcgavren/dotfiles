TODO restore!
%w{rubygems active_record}.each do |lib|
  begin
    require lib
  rescue LoadError => err
    $stderr.puts "Couldn't load #{lib}: #{err}"
  end
end

Output SQL queries, etc. to terminal.  (Via Ryan Bates)
ActiveRecord::Base.logger = Logger.new(STDOUT)
