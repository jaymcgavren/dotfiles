#!/usr/bin/env ruby
$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'ruby', 'utility', 'lib'))
require 'utility'

util = Util.new
result = util.load(:storage)[ARGV.join(' ')] or fail "Could not load"
print result